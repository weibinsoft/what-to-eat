# 今天吃什么 - Android 版

使用 Kotlin 和 Jetpack Compose 开发的 Android 客户端。

## 功能特性

- 🎰 **随机决策** - 老虎机动画效果，帮你决定今天吃什么
- 📋 **菜单管理** - 添加、删除餐厅和菜品
- 📜 **历史记录** - 查看过往决策记录
- ⚙️ **服务器配置** - 可自定义后端服务器地址
- 🎨 **Material 3 设计** - 现代化 UI 界面

## 技术栈

| 技术 | 说明 |
|------|------|
| Kotlin | 开发语言 |
| Jetpack Compose | 声明式 UI 框架 |
| Material 3 | UI 设计规范 |
| Hilt | 依赖注入 |
| Retrofit | 网络请求 |
| DataStore | 本地存储 |
| Coroutines + Flow | 异步处理 |
| Navigation Compose | 页面导航 |

## 环境要求

- Android Studio Hedgehog (2023.1.1) 或更高版本
- JDK 17
- Android SDK 34
- 最低支持 Android 8.0 (API 26)

## 快速开始

### 1. 使用 Android Studio 打开项目

```bash
# 打开 android 目录
cd android
```

在 Android Studio 中选择 `File -> Open`，然后选择 `android` 目录。

### 2. 配置服务器地址

启动应用后，在登录页点击右上角 ⚙️ 设置图标，配置后端服务器地址：

- **模拟器连接本地服务器**: `http://10.0.2.2:8080`
- **真机连接本地服务器**: `http://<电脑IP>:8080`
- **远程服务器**: `http://your-server.com:8080`

> ⚠️ 注意: Android 模拟器中 `localhost` 指向模拟器本身，需要使用 `10.0.2.2` 来访问宿主机的 localhost。

### 3. 构建和运行

```bash
# 使用 Gradle 构建
./gradlew assembleDebug

# 安装到设备
./gradlew installDebug
```

或直接在 Android Studio 中点击 Run 按钮。

## 项目结构

```
android/
├── app/
│   ├── build.gradle.kts          # 应用构建配置
│   └── src/main/
│       ├── AndroidManifest.xml   # 应用清单
│       ├── java/com/whattoeat/
│       │   ├── WhatToEatApplication.kt  # Application 类
│       │   ├── MainActivity.kt          # 主 Activity
│       │   ├── data/
│       │   │   ├── api/                 # API 服务和模型
│       │   │   ├── datastore/           # DataStore 配置存储
│       │   │   └── repository/          # 数据仓库
│       │   ├── di/                      # 依赖注入模块
│       │   ├── ui/
│       │   │   ├── navigation/          # 导航配置
│       │   │   ├── screens/             # 页面组件
│       │   │   └── theme/               # 主题配置
│       │   └── viewmodel/               # ViewModel
│       └── res/                         # 资源文件
├── build.gradle.kts              # 根构建配置
├── settings.gradle.kts           # 设置
└── gradle.properties             # Gradle 属性
```

## 页面说明

### 启动页 (SplashScreen)
- 检查登录状态
- 自动跳转到登录页或主页

### 登录页 (LoginScreen)
- 支持登录和注册
- 服务器配置入口

### 主页 (HomeScreen)
- **决策 Tab**: 老虎机动画，随机选择菜品
- **菜单 Tab**: 管理餐厅和菜品列表
- **历史 Tab**: 查看决策历史记录

### 设置页 (SettingsScreen)
- 配置服务器地址
- 提供常用配置说明

## 构建发布版

```bash
# 构建 Release APK
./gradlew assembleRelease

# APK 位置
# app/build/outputs/apk/release/app-release-unsigned.apk
```

## 注意事项

1. 确保后端服务器已启动并可访问
2. Android 9+ 默认禁止明文 HTTP，已在 Manifest 中配置 `usesCleartextTraffic="true"`
3. 修改服务器地址后需要重启应用生效

## License

MIT
