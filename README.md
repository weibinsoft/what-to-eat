# 今天吃什么 - What To Eat

一个具有"仪式感"的午餐决策 App，帮助选择困难症患者做出今天吃什么的决定。

## 功能特性

- 🎰 老虎机风格的随机决策动画 + 音效
- 🍜 餐厅和菜品管理（支持自动补全餐厅名）
- 📊 最近5天用餐历史记录
- 🎯 加权随机算法（最近3次吃过的菜品概率降低50%）
- 🔐 用户登录/注册（JWT认证）
- 📱 响应式设计，支持移动端

## 技术栈

| 层级 | 技术 |
|------|------|
| **前端** | Vue 3 + TypeScript + Vite + Pinia + Vue Router |
| **后端** | Go 1.21+ + Gin + GORM + Zap日志 |
| **数据库** | MySQL 8.0 |
| **部署** | Docker Compose |

## 快速开始

### 方式一：Docker Compose（推荐）

```bash
# 启动 MySQL
docker-compose up -d

# 启动项目
chmod +x start.sh
./start.sh
```

### 方式二：一键启动脚本

确保 MySQL 服务已启动，编辑 `backend/config/config.yaml` 配置数据库连接，然后：

```bash
./start.sh start     # 启动后端和前端（默认）
./start.sh stop      # 停止所有服务
./start.sh restart   # 重启所有服务
./start.sh backend   # 只启动后端
./start.sh frontend  # 只启动前端
./start.sh help      # 显示帮助
```

### 方式三：手动启动

#### 1. 启动后端

```bash
cd backend
go mod tidy
go run cmd/server/main.go
```

后端会自动创建数据库、表结构和默认数据。

#### 2. 启动前端

```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:5173

## 使用说明

1. 注册账号并登录
2. 在右侧菜单列表添加餐厅和菜品
   - 输入餐厅名（支持自动补全已有餐厅）
   - 输入菜品名
   - 点击添加
3. 点击"开始决策"按钮
4. 观看老虎机动画，享受决策过程
5. 查看历史记录

## API 接口

### 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |

### 菜单管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/menus` | 获取菜单列表 |
| POST | `/api/menus` | 添加菜品（同时处理餐厅） |
| DELETE | `/api/menus/:id` | 删除菜品 |
| GET | `/api/restaurants` | 获取餐厅列表（用于下拉选择） |

### 决策

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/decide` | 执行随机决策 |
| GET | `/api/history` | 获取最近5天的历史记录 |

## 加权随机算法

为了避免用户连续多天吃同样的食物，系统实现了加权随机算法：

```
正常菜品权重: 1.0
最近3次吃过的菜品权重: 0.5 (概率降低50%)
```

## 数据库设计

```
┌─────────────┐     ┌─────────────┐     ┌──────────────────┐
│   users     │     │ restaurants │     │ decision_records │
├─────────────┤     ├─────────────┤     ├──────────────────┤
│ id          │     │ id          │     │ id               │
│ username    │     │ name        │     │ user_id          │
│ password    │     │ created_at  │     │ menu_id          │
│ created_at  │     │ updated_at  │     │ decided_at       │
│ updated_at  │     └─────────────┘     └──────────────────┘
└─────────────┘            │
                           │ 1:N
                           ▼
                    ┌─────────────┐
                    │   menus     │
                    ├─────────────┤
                    │ id          │
                    │ restaurant_id│
                    │ dish_name   │
                    │ created_at  │
                    │ updated_at  │
                    └─────────────┘
```

## 项目结构

```
.
├── start.sh                 # 一键启动脚本
├── docker-compose.yml       # Docker Compose 配置
├── frontend/                # Vue 3 前端
│   ├── src/
│   │   ├── api/            # API 请求封装
│   │   ├── components/     # 组件（SlotMachine, MenuList, HistoryPanel）
│   │   ├── stores/         # Pinia 状态管理
│   │   ├── views/          # 页面（Login, Home）
│   │   ├── utils/          # 工具函数（音效管理）
│   │   └── router/         # 路由配置
│   └── package.json
├── backend/                 # Go 后端
│   ├── cmd/server/         # 入口文件
│   ├── config/             # Viper 配置
│   ├── internal/
│   │   ├── handler/        # HTTP 处理器
│   │   ├── service/        # 业务逻辑（含加权随机算法）
│   │   ├── repository/     # 数据访问（含数据库初始化）
│   │   └── model/          # 数据模型
│   └── pkg/
│       ├── middleware/     # JWT认证、CORS、日志中间件
│       └── logger/         # Zap 日志封装
├── sql/
│   └── init.sql            # 数据库初始化脚本（备用）
└── README.md
```

## 配置说明

### 后端配置 (`backend/config/config.yaml`)

```yaml
server:
  port: "8080"
  mode: "debug"

database:
  host: "127.0.0.1"
  port: "3306"
  user: "root"
  password: "root123456"
  dbname: "what_to_eat"

jwt:
  secret: "your-secret-key"
  expire_time: 24

log:
  level: "info"
  format: "console"
  output_path: "stdout"
```

支持环境变量覆盖（前缀 `APP_`）：
- `APP_DATABASE_HOST`
- `APP_DATABASE_PASSWORD`
- `APP_JWT_SECRET`

### Docker Compose

```bash
# 启动 MySQL
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

MySQL 配置：
- 端口: 3306
- 用户: root / root123456
- 数据库: what_to_eat

## 运行测试

```bash
cd backend
go test ./... -v
```

## License

MIT
